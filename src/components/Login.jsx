import UsersList from "./UsersList";

export default function Login() {
  return (
    <section id="login-page">
      <h2 id="login-header">Select Your Account</h2>
      <UsersList />
    </section>
  );
}
